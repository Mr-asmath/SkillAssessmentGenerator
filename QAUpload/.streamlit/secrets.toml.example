# Copy this file to .streamlit/secrets.toml locally or configure these
# values in Streamlit Community Cloud secrets.

GEMINI_API_KEY = "your-gemini-api-key"

# Optional: override the default model used by the app.
# GEMINI_MODEL = "models/gemini-2.5-flash"
